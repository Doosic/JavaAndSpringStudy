spring:
#  main:
#    web-application-type: none
  h2:
    console:
      enabled: true
  jpa:
    show_sql: true
    properties:
      hibernate:
        format_sql: true
        use_sql_commets: true
# h2 데이터베이스도 config 파일없이 이곳에서 설정할 수 있다.
#  datasource:
#    driver-class-name: org.H2.Driver
#    username: userName
#    password: 1234

logging.level:
  org.hibernate.SQL: debug

#debug: true